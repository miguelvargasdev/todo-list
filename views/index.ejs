<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://kit.fontawesome.com/7939ca6c72.js" crossorigin="anonymous"></script>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
    <link rel="stylesheet" href="css/style.css">

    <title>Daily Task List</title>
</head>
<body>
    <nav class="grey lighten-1" role="navigation"></nav>
    <div class="center">
    <div class="container">
        <h1>Today's Task List: </h1>
        <% if(items.length > 0){ %>
        <ul class="todoItems collection">
                <% for(let i=0; i < items.length; i++) {%>
                    <li class="item collection-item ">
                        <% if(items[i].completed === true) {%>
                            <span class="task completed" id=<%= items[i]._id%>><%= items[i].task %></span>
                        <% }else{ %>
                            <span class="task"id=<%= items[i]._id%>><%= items[i].task %></span>
                        <% } %>
                        <span class="badge close"><i class="material-icons col s3 ">close</i></span>  
                    </li>
                    <% } %>         
        </ul>
        <% } %>
        <h2>Left to do: <%= left %></h2>

        <h2>Add a Todo:</h2>
        <form action="/addTodo" method="POST">
            <input type="text" placeholder="Add item here.." name="todoItem">
            <input type="submit">
        </form>
    </div>

</div>
    
    <script src='js/main.js'></script>
</body>
</html>